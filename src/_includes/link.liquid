<li class="link">
    <span>{{assetsDir}}</span>
    <img src="assets/{{faviconDict[raindrop._id]}}" />
    <a href="{{raindrop.link}}">
        {{raindrop.title}}
        {% if raindrop.excerpt %}
        <p class="description">
            {{raindrop.excerpt}}
        </p>
        {% endif %}
    </a>
    {% if raindrop.note %}
    <blockquote class="note">
        {{raindrop.note}}
    </blockquote>
    {% endif %}
</li>
